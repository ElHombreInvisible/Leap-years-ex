#include <stdio.h>
//добавить необходимые заголовочные файлы
int is_leap(int year);
int closest_leap_year(int year);
void fill_list_of_years(int* allocated); // Упрощение, не принимать за истину


int main(void) {
    // КОД СЮДА
    return 0;
}